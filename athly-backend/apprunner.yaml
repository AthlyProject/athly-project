version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm ci
    build:
      - npm run build
run:
  command: node dist/src/main.js
  network:
    port: 4000
  env:
    - name: NODE_ENV
      value: production
