VITE_BACKEND_API_URL="http://localhost:4000"
# Set to "true" to expose the /register route and the "Criar conta" link.
# Leave unset/false in production during the waitlist phase.
VITE_ENABLE_REGISTRATION="true"
