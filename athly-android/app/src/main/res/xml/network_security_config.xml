<?xml version="1.0" encoding="utf-8"?>
<!-- Produção: HTTPS apenas (sem cleartext). O override de debug (src/debug) permite HTTP local. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
